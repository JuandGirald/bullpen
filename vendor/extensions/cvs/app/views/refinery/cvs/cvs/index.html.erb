<% content_for :body do %>
  <ul id="cvs">
    <% @cvs.each do |cv| %>
      <li>
        <%= link_to cv.name, refinery.cvs_cv_path(cv) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
