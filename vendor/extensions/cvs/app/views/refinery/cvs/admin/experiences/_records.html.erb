<%= render 'refinery/admin/search_header', :url => refinery.cvs_admin_experiences_path %>
<div class='pagination_container'>
  <% if @experiences.any? %>
    <%= render 'experiences' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
